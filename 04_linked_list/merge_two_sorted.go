package linkedlist_practice

// MergeTwoLists merges two sorted linked lists and returns the sorted result.
// Input: 1→2→4, 1→3→4  →  1→1→2→3→4→4
func MergeTwoLists(l1, l2 *Node) *Node {
	// TODO: implement (iterative with dummy head, O(n+m))
	return nil
}
