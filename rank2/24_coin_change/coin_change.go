package coin_change

// CoinChange returns the fewest coins needed to make up amount,
// or -1 if it's not possible. Coins may be reused.
// Input: coins=[1,5,11], amount=15  →  3  (5+5+5)
func CoinChange(coins []int, amount int) int {
	// TODO: implement (bottom-up DP, O(amount * len(coins)))
	return -1
}
