package first_nonrepeating

// FirstNonRepeating returns the index of the first character in s that does
// not appear more than once. Returns -1 if every character repeats or s is empty.
// Example: "loveleetcode" → 2  (the 'v')
func FirstNonRepeating(s string) int {
	// TODO: implement
	return -1
}
