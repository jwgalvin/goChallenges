package arrays_practice

// ProductExceptSelf returns an array where output[i] is the product of
// all elements except nums[i]. Must run in O(n) without using division.
// Input: [1,2,3,4]  →  [24,12,8,6]
func ProductExceptSelf(nums []int) []int {
	// TODO: implement (prefix pass then suffix pass)
	return nil
}
